language: c
install:
  - curl https://static.rust-lang.org/rustup.sh | sudo bash
script:
  - cargo build --verbose
  - cargo test --verbose
notifications:
  email:
    recipients:
      - franklinchen@franklinchen.com
    on_success: never
